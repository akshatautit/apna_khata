# Backend API URL — leave empty for local dev (Vite proxy handles /api)
# On Render / production, set this to your backend's full URL:
# VITE_API_URL=https://khata-ai-backend.onrender.com
VITE_API_URL=
